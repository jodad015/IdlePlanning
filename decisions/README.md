# Decision Records

When a concrete design choice is made, record it here so it doesn't get relitigated.

Format: `NNN-topic-name.md` (e.g., `001-core-loop-style.md`)

Each record captures: context, options considered, the decision, and rationale.
